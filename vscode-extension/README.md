# Remote Control Bridge

This extension acts as a bridge between VSCode and the Remote IDE Control Server.

## Features
- Connects to the local Control Server (default: `http://localhost:3000`).
- Allows remote listing, creation, and control of VSCode terminals.

## Requirements
- The Remote IDE Control Server must be running.
